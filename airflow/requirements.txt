# Pipeline dependencies installed inside the Airflow image.
# Keep this in sync with pyproject.toml (we use pinned-light upper bounds to
# stay reproducible without locking ourselves out of patch upgrades).
duckdb>=1.1,<2.0
dbt-core>=1.8,<2.0
dbt-duckdb>=1.8,<2.0
click>=8.1
structlog>=24.1
faker>=25.0
