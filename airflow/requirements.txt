# Pipeline dependencies installed inside the Airflow image.
# Keep in sync with pyproject.toml — same lower bounds, light upper bounds
# for reproducibility without blocking patch upgrades.
duckdb>=1.1,<2.0
dbt-core>=1.9,<2.0
dbt-duckdb>=1.9,<2.0
click>=8.1
structlog>=24.1
faker>=30.0
python-dateutil>=2.9
pydantic>=2.6
